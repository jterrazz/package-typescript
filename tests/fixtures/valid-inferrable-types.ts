// Valid: inferrable types should be allowed (typescript/no-inferrable-types disabled)
const count: number = 5;
const name: string = "test";
const active: boolean = true;

export { active, count, name };
