# Usage

Call `greet` to build a message and `add` to sum two numbers.

```ts
import { greet, add } from 'sample-documented';

greet('Ada');
add(2, 3);
```

Log severity is described by the `Level` enum.
