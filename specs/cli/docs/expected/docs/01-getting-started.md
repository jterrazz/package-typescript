# Getting started

Install the library and import the public API from the package entry point.

```bash
npm install sample-documented
```

Everything the library exposes is re-exported from `src/index.ts`.
