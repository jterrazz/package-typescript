description: runs the Docs sync pass once the project has committed docs
fixture: $FIXTURES/sample-documented/
runs:
    - command: docs
      exit: 0
      stdout: |4
           TYPESCRIPT  Generating API docs...

          [info] Loaded plugin typedoc-plugin-markdown
          [info] markdown generated at ./docs/reference
          Generated docs/reference/

          Docs generated at docs/
    - command: check
      exit: 1
      stdout: |4
           START  Running quality checks

           RUN  TypeScript Check

          ✓ Passed

           RUN  Oxlint Check

          ✓ Passed

           RUN  Oxfmt Check

          Checking formatting...

          docs/02-usage.md ({{duration}})
          docs/reference/enumerations/Level.md ({{duration}})
          docs/reference/functions/add.md ({{duration}})
          docs/reference/functions/greet.md ({{duration}})
          docs/reference/index.md ({{duration}})
          docs/reference/type-aliases/User.md ({{duration}})
          docs/reference/variables/VERSION.md ({{duration}})
          package.json ({{duration}})
          src/index.ts ({{duration}})
          tsconfig.json ({{duration}})

          Format issues found in above 10 files. Run without `--check` to fix.
          Finished in {{duration}} on 12 files using {{int}} threads.
          No config found, using defaults. Please add a config file or try `oxfmt --init` if needed.
          ✗ Failed with exit code 1

           RUN  Knip (unused code)

          ✓ Passed

           RUN  Docs (sync)

          ✓ Passed

           END  Finalizing quality checks

          ✗ Some checks failed
