description: fails a module graph that crosses the layer map the project declared
fixture: architecture-map/crossed/
runs:
    - command: check
      exit: 1
      stdout: |4
           START  Running quality checks

           RUN  TypeScript Check

          ✓ Passed

           RUN  Oxlint Check

          ✓ Passed

           RUN  Oxfmt Check

          ✓ Passed

           RUN  Knip (unused code)

          ✓ Passed

           RUN  Architecture (layer map)


            error domain-stays-pure: src/domain/user.ts → src/infrastructure/store.ts
              A domain module never reaches for infrastructure.


          x 1 dependency violations (1 errors, 0 warnings). 2 modules, 1 dependencies cruised.

          ✗ Failed with exit code 1

           END  Finalizing quality checks

          ✗ Some checks failed
