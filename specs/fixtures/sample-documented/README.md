# sample-documented

A sample documented library used to exercise the `typescript docs` compiler.

It ships a small public API and a hand-written `docs/` corpus so the generated
reference tree has real content to compile.
